name: note
on: push
jobs:
  all:
    env:
      GO111MODULE: on
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-go@v2
        with:
          go-version: '1.16.3'
      - run: go build -v
